Enter file contents here#include "stdio.h" 
void main() 
{ 
int i; 
long int a[40]; 
a[0]=a[1]=1; 
for(i=2;i<40;i++) 
{ 
a[i]=a[i-2]+a[i-1]; 
} 
for(i=0;i<40;i++) 
{ 
if(i%5==0) 
printf("\n"); 
printf("%12d",a[i]); 
} 
printf("\n"); 

}
